This program scans STDIN and adds any found IP addresses to a pf table. For
more information, please see man 1 block.

To install:

$ make
$ sudo sh -c 'PREFIX=/usr/local make install'

For manual:

$ man 1 block
